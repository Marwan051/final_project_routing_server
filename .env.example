# Example environment variables for the routing server
# Copy this to .env and fill in your actual values

# PostgreSQL Database Configuration
PG_DB_NAME=routing_db
PG_USER=routing_user
PG_PASSWORD=your_secure_password
PG_HOST=localhost
PG_PORT=5432

# Docker Compose will use these values if .env exists
